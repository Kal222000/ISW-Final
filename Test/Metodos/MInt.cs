using System;
using System.Text.RegularExpressions;

namespace Test.Metodos
{
    public static class MInt
    {
        public static bool Carnet(int n)
        {
            if(n <= 0) {  return false; }
            else { return true; }
        }
    }
}